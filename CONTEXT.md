# FanBox

FanBox 以「原件 + 接入」模型管理本机 Skills：每个 Skill 只有一份原件，各 Agent 通过接入引用它，FanBox 让用户看清每个 Agent 能看到什么，并一处维护、逐 Agent 启停。

## Language

**Skill**:
由名称表达的一项 Agent 能力；对应一个原件和各 Agent 的接入状态，不再以多副本形式存在。

**原件**:
一个 Skill 的权威内容目录，是所有引用最终指向的真实目录；由 FanBox 管理的位于原件仓（仓库家族除外），由外部工具管理的留在其自身目录。更新原件即对全部已接入 Agent 生效。
_Avoid_: 副本、安装副本

**原件仓**:
`~/.agents/skills`，FanBox 存放自管原件的目录，仓库家族以相对软链入仓；Codex 与 ZCode 原生扫描它，Claude 与 WorkBuddy 通过接入引用其中的原件。

**仓库家族**:
原件位于本仓库 `.agents/skills/` 的 Skill 集合；在原件仓中呈现为逐条相对软链，git 是其唯一真源，改仓库即对全部已接入 Agent 生效。
_Avoid_: 项目级 skills

**Skill 条目**:
外部来源中可被发现和评估、但尚未安装到本机的 Skill 记录；安装成功后才产生原件。
_Avoid_: 外部 Skill 安装项、搜索结果

**发现**:
从受支持的外部来源搜索和浏览 Skill 条目，以便用户评估并选择是否安装。
_Avoid_: 探索、外部导入

**安装**:
用户确认 Skill 条目、接入对象和风险信息后，将固定版本的外部内容安全地创建为原件仓中的原件，并按所选 Agent 建立接入；不选任何 Agent 的纯库存安装同样合法。不自动重启 Agent。
_Avoid_: 导入、下载、部署

**纯库存**:
只存在于原件仓、当前没有任何 Agent 接入的原件；它是合法的安装形态，等待用户逐步接入。

**风险检查**:
内容成为原件前（安装与收编）由 FanBox 执行的同一套客观静态检查；它允许普通二进制资源但拒绝链接、特殊文件和未知可执行二进制，并对脚本、高权限工具及未知风险加强确认，不包含第三方审计，也不代表 FanBox 对 Skill 安全性的认证或担保。
_Avoid_: 安全认证、安全保证、健康检查

**健康检查**:
对本机原件与接入现值的持续只读检查：断链、死环、WorkBuddy 拷贝漂移、已被外部修改、描述预算与截断等；它报告事实并提供处置入口，不自动修复，也不评判未入仓内容的安全风险。
_Avoid_: 风险检查

**来源身份**:
能够唯一指向外部 Skill 条目的公开仓库和 Skill 子目录组合；名称不属于来源身份，同名条目可以拥有不同来源身份。
_Avoid_: Skill 名称、下载地址

**同源更新**:
使用同一来源身份的新固定版本替换原件；更新必须重新执行风险检查，向用户呈现受影响的接入范围后方可执行，旧内容保持可恢复。因各 Agent 引用同一原件，更新对全部已接入 Agent 同时生效。
_Avoid_: 同名更新、自动更新

**接入**:
让某 Agent 的后续会话能够发现一个原件的动作；接入形态随 Agent 原生机制而异（软链、配置或拷贝），从未接入与取消接入在界面上不做区分。
_Avoid_: 导入、复制、启用

**取消接入**:
让某 Agent 不再发现一个原件但原件保持不变；已有会话不被撤回，部分 Agent 需重启后生效。
_Avoid_: 停用、禁用

**收编**:
把散落在原件仓之外的本机 Skill 拷贝（残留、项目级目录等）提升为原件仓中的原件，提升前执行与安装同一套风险检查；收编后各 Agent 逐步改为接入，取代旧「导入」的复制语义。
_Avoid_: 导入、复制、移动

**残留**:
Agent 的 Skill 目录中不属于任何接入的本地实体，多为历史拷贝或散落文件；是收编或清理的对象。
_Avoid_: 垃圾文件、孤儿副本

**外来原件**:
原件仓中没有来源身份记录的原件（收编件、第三方安装器或手动放入）；与有来源身份的原件同权管理接入与卸载，但不提供同源更新。
_Avoid_: 野原件、未知原件

**目标 Agent**:
接入的接收方；首批为 Claude、Codex、ZCode 和 WorkBuddy。原件仓本身是 Codex 与 ZCode 的原生发现位置，不作为目标 Agent。

**内容冲突**:
接入时，目标 Agent 位置已存在与原件同名的实体。仅 Skill 名称相同但目录名不同属于名称歧义，不视为可覆盖的内容冲突。
_Avoid_: 重复 Skill

**覆盖**:
用户确认内容冲突后，以原件内容替换目标位置的同名实体；它不是逐文件合并，原目标内容应保持可恢复。
_Avoid_: 合并、更新

**接管**:
用户确认覆盖后，FanBox 的来源身份记录取代被覆盖原件所持身份（含无记录的外来原件）的过程；被接管的旧内容保持可恢复，接管后的原件方可同源更新。
_Avoid_: 合并、静默替换

**卸载**:
对原件仓中的原件：将原件移入系统废纸篓并取消全部接入；对外部原件：仅取消全部接入，不触碰外部内容。均非永久删除，可由用户恢复。
_Avoid_: 永久删除、清除

**批量管理**:
用户选择多个 Skill 或整个 Agent 列表达一次接入、取消接入或卸载意图的临时模式；每个对象独立产生操作结果。
