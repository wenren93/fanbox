# FanBox

FanBox 汇集并管理分布在不同 Agent、插件和项目目录中的 Skills，让用户能够查看每个安装副本并控制其可见性。

## Language

**Skill**:
由名称表达的一项 Agent 能力；同一个 Skill 可以同时存在多个安装项。

**Skill 安装项**:
Skill 在某一来源和目录中的具体安装副本，是选择、启停和卸载的最小对象。
_Avoid_: Skill 副本、Skill 行

**目标 Agent**:
用户为 Skill 导入选择的接收方；首批可选项是 Claude、Codex、Agents 和 WorkBuddy 的全局 Skill 位置。

**导入**:
从一个已有的有效 Skill 安装项完整复制出目标 Agent 的新安装项；源安装项保持不变，两个安装项此后相互独立。每次导入只选择一个目标 Agent。
_Avoid_: 移动、同步、共享

**目标安装项**:
导入在目标 Agent 中创建的 Skill 安装项；即使来源由插件管理或处于停用状态，它也作为独立的普通安装项存在。

**内容冲突**:
导入时，目标 Agent 已存在与来源目录同名的 Skill 安装项。仅 Skill 名称相同但目录名不同属于名称歧义，不视为可覆盖的内容冲突。
_Avoid_: 重复 Skill

**覆盖**:
用户确认内容冲突后，以来源的完整内容替换目标安装项；它不是逐文件合并，原目标安装项应保持可恢复。
_Avoid_: 合并、更新

**启用**:
让一个 Skill 安装项在对应 Agent 的配置中处于可用状态，因而可被后续 Agent 会话发现。Claude 按 Skill 名称配置，Codex 按 `SKILL.md` 绝对路径配置；WorkBuddy 则把安装项放在 `skills` 目录。

**停用**:
让一个 Skill 安装项在对应 Agent 的配置中变为不可用但仍保留在本机，是可逆状态；后续 Agent 会话将不再发现它，但已有会话不会被撤回。Codex 需要重启后生效。Claude 的设置按名称生效，因此同名普通安装项会同步变化；WorkBuddy 把安装项移到与 `skills` 同级的 `skills_disabled`；插件 Skill 由插件管理。
_Avoid_: 关闭、删除

**卸载**:
将一个 Skill 安装项移到系统废纸篓；它不是永久删除，仍可由用户从废纸篓恢复。
_Avoid_: 永久删除、清除

**批量管理**:
用户选择多个 Skill 安装项并对它们表达一次启用、停用或卸载意图的临时模式；每个安装项独立产生操作结果。
